apiVersion: v1
kind: Service
metadata:
  name: task-api-service
  labels:
    app: task-api
spec:
  type: NodePort
  selector:
    app: task-api
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
      nodePort: 30080
      name: http
  sessionAffinity: None
